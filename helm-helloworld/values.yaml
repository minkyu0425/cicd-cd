#1
printValues: "hello world2!"
nameOverride: "helm-helloworld"

ingress:
  enabled: true
  className: ""
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.global-static-ip-name: gke-static-ip
  path: /
  hosts:
  - host: 172.16.0.2

service:
  type: ClusterIP
  port: 8080

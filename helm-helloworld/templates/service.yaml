apiVersion: v1
kind: Service
metadata:
  name: helloworld
  labels:
    app: helloworld
    service: helloworld
spec:
  replicas: 1
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: http
  selector:
    app: helloworld

#1

replicaCount: 1

image:
  repository: asia-northeast3-docker.pkg.dev/test-project-11542/cicd/dockerfile:v1
  pullPolicy: IfNotPresent
  tag: "v1"
  # Overrides the image tag whose default is the chart appVersion.
  
printValues: "hello world2!"
nameOverride: "helm-helloworld"
fullnameOverride: "helm-helloworld"

ingress:
  enabled: true
  className: ""
  annotations:
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
    kubernetes.io/ingress.global-static-ip-name: gke-static-ip
  path: /
  hosts:
  - host: 172.16.0.2

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: "helm-helloworld"
  
service:
  type: ClusterIP
  port: 80
